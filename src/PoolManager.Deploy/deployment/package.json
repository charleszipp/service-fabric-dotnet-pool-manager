{
  "name": "PoolManager",
  "shortname": "pools",
  "version": "$VERSION$",
  "appcentral": true,
  "resourcegroup": false,
  "globaltokens": {
    "applicationInsightsName": "[AzCreateGlobalAppInsightsNameForPackage('audit-infrax')]",
    "primary_region": "[getPrimaryRegion()]",
    "primary_region_rg": "[AzCreateResourceGroupForRegionAndPackage(token('primary_region'), 'audit-infrax')]"
  },
  "deployments": [
    {
      "type": "all",
      "value": {
        "commands": [ "[Deploy-Azure()]" ],
        "tokens": {
          "AppInsightsInstrumentationKey": "[GetAppInsightsInstrumentationKey(token('applicationInsightsName'),token('primary_region_rg'))]"
        },
        "templates": {
        },
        "tollbridge": {
          "config": [
          ],
          "tokens": {
          }
        },
        "packages": {
          "PoolManager": {
            "type": "asf",
            "target": "[GetGraphCluster('1')]",
            "appname": "[concat('fabric:/','PoolManager')]"
          }
        }
      }
    }
  ],
  "dependencies": {
  }
}